# Risk Checklist Rules

`risk_checklist.json` must be a JSON object. Include `critical_failures` as a
list and add manual checks for deck truth, reagent sufficiency, labware identity,
and any risks that cannot be proven by simulation.
