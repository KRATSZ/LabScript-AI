# Legacy protocol demo inputs

Small sample files formerly at the repository root. Not part of the v0.4 three-piece package contract (`protocol.py`, `setup_card.html`, `manifest.json`); kept only as optional examples.
